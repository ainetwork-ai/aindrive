node_modules
.next
dist
*.log
.DS_Store
# Env: never commit real values. Only *.example templates are tracked
# (the negations below re-include them). Covers .env, .env.local,
# .env.production, .env.prod, .env.*.local, etc.
.env
.env.*
!.env.example
!.env.*.example
# 1Password op-inject templates hold only op:// references (no secrets) — commit.
!.env.*.tpl
.aindrive
sample/

# Tooling artifacts
.omc/
.playwright-mcp/
.claude/
*.tsbuildinfo
next-env.d.ts

# Test artifacts
playwright-report/
test-results/
*.png
!docs/**/*.png
.worktrees/
.gstack/

# brainstorming visual companion (local only)
.superpowers/
